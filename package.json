{
  "name": "linda-slack-bridge",
  "version": "1.0.0",
  "description": "Bridge so an ElevenLabs Agent (Linda) participates in a Slack channel, replying to every message with judgment.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0"
  }
}
